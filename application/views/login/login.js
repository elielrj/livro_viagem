function validacaoFormulario(formulario){

    if(formulario.senha.value == '9524' && formulario.email.value == 'elielrj@gmail.com'){
        alert("Login efetuado com sucesso!");
            //confirm(index.php);            
        }else{ 
            alert("Dados incorretos!");
        }
}

